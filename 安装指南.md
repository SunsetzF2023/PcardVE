# 🐍 Python安装和游戏运行指南

## 📋 Windows系统安装Python

### 方法一：官方安装（推荐）

#### 1. 下载Python
- 访问官网：https://www.python.org/downloads/
- 点击 "Download Python 3.x.x" 按钮（最新版本）
- 保存安装文件到桌面

#### 2. 安装Python
- 双击下载的 `.exe` 文件
- **重要**：勾选 "Add Python to PATH" 选项
- 点击 "Install Now" 开始安装
- 等待安装完成

#### 3. 验证安装
- 按 `Win + R` 键，输入 `cmd`，按回车
- 在黑色窗口中输入：`python --version`
- 如果显示版本号，说明安装成功

### 方法二：Microsoft Store安装

#### 1. 打开Microsoft Store
- 点击开始菜单
- 搜索 "Microsoft Store"
- 打开应用

#### 2. 搜索Python
- 在Store中搜索 "Python"
- 选择 "Python 3.x"（通常有免费版本）
- 点击 "获取" 或 "安装"

## 🎮 运行游戏

### 方法一：使用命令行（推荐）

#### 1. 打开命令提示符
- 按 `Win + R` 键
- 输入 `cmd`，按回车
- 出现黑色命令窗口

#### 2. 进入游戏目录
```cmd
cd C:\Users\jefffan\Desktop\cardpve
```

#### 3. 运行游戏
```cmd
python main.py
```

### 方法二：直接双击运行

#### 1. 创建批处理文件
- 在游戏文件夹中新建文本文档
- 输入以下内容：
```batch
@echo off
cd /d "%~dp0"
python main.py
pause
```
- 保存为 `启动游戏.bat`

#### 2. 运行游戏
- 双击 `启动游戏.bat` 文件
- 游戏会自动启动

### 方法三：使用PowerShell

#### 1. 右键菜单
- 在游戏文件夹中按住 `Shift` 键
- 右键点击空白处
- 选择 "在此处打开PowerShell窗口"

#### 2. 运行游戏
```powershell
python main.py
```

## 🔧 常见问题解决

### 问题1：提示"python不是内部或外部命令"
**解决方案：**
1. 重新安装Python，确保勾选"Add Python to PATH"
2. 或者使用 `py` 命令代替 `python`：
```cmd
py main.py
```

### 问题2：提示"无法识别Python"
**解决方案：**
1. 确认Python已正确安装
2. 重启电脑后再试
3. 检查环境变量设置

### 问题3：游戏窗口一闪而过
**解决方案：**
1. 使用批处理文件运行（包含pause命令）
2. 或者在命令行中运行，可以看到错误信息

## 📱 替代方案：在线Python

如果不想安装Python，可以使用在线Python环境：

### 1. Replit网站
- 访问：https://replit.com/
- 注册账号
- 创建新的Python项目
- 复制游戏代码到网站

### 2. Google Colab
- 访问：https://colab.research.google.com/
- 新建笔记本
- 上传游戏文件

## 🎯 最简单的方法

### 一键启动脚本
我已经为你创建了启动脚本：

1. **找到游戏文件夹**：`C:\Users\jefffan\Desktop\cardpve`
2. **双击运行**：`启动游戏.bat`

如果文件不存在，请按以下步骤创建：

1. 在文件夹中右键 → 新建 → 文本文档
2. 重命名为 `启动游戏.bat`
3. 右键编辑，粘贴以下内容：
```batch
@echo off
title 植物大战僵尸英雄 - 卡牌对战
echo 正在启动游戏...
cd /d "%~dp0"
python main.py
if errorlevel 1 (
    echo.
    echo 启动失败！请检查Python是否已安装。
    echo.
    echo 如果没有安装Python，请访问：
    echo https://www.python.org/downloads/
    echo.
    pause
)
```

## 📞 需要帮助？

如果遇到问题，可以：
1. 截图错误信息
2. 记录具体的错误提示
3. 联系我寻求帮助

## 🎮 游戏控制

运行游戏后：
- 使用键盘输入数字选择菜单选项
- 输入卡牌名称进行搜索
- 按回车键确认操作
- 输入 `5` 可以退出游戏

祝你游戏愉快！🌻🧟
